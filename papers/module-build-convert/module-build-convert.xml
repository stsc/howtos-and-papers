<?xml version="1.0" encoding="utf-8"?>

<!DOCTYPE article PUBLIC "-//OASIS//DTD DocBook XML V4.1.2//EN"
 "http://www.oasis-open.org/docbook/xml/4.1.2/docbookx.dtd">

<article>

<articleinfo>
<title>Module::Build::Convert</title>
<authorgroup>
<author>
<firstname>Steven</firstname>
<surname>Schubiger</surname>
</author>
</authorgroup>
</articleinfo>

<para>Copyright (c) 2005-2008, 2024-2025 Steven Schubiger</para>
<para>
Permission is granted to copy, distribute and/or modify this document
under the terms of the GNU Free Documentation License, Version 1.2
or any later version published by the Free Software Foundation;
with no Invariant Sections, no Front-Cover Texts, and no Back-Cover
Texts.  A copy of the license is included in the section entitled "GNU
Free Documentation License".
</para>

<sect1>
<title>Common distribution "skeleton"</title>

<para>
It has been common practice within the Perl community for ages to ship
distributions with a <filename>Makefile.PL</filename> so that the user will be able to install 
the packages when he retrieves them either via the shell which the <command>CPAN/CPANPLUS</command>
modules offer or via manual CPAN download.
</para>

<para>
The <filename>Makefile.PL</filename> consists of "meta information" which in the case of the 
distribution <command>HTML::Tagset</command> looks as follows:
</para>

<example>
<title></title>
<programlisting>
# This -*-perl-*- program writes the Makefile for installing this distribution.
#
# See "perldoc perlmodinstall" or "perldoc ExtUtils::MakeMaker" for
# info on how to control how the installation goes.

require 5.004;
use strict;
use ExtUtils::MakeMaker;

WriteMakefile(
    NAME            => 'HTML::Tagset',
    AUTHOR          => 'Andy Lester &lt;andy@petdance.com&gt;',
    VERSION_FROM    => 'Tagset.pm', # finds $VERSION
    ABSTRACT_FROM   => 'Tagset.pm', # retrieve abstract from module
    PMLIBDIRS       => [qw(lib/)],
    dist            => { COMPRESS => 'gzip -9f', SUFFIX => 'gz', },
    clean           => { FILES => 'HTML-Tagset-*' },
);
</programlisting>
</example>

<para>
Of interest are the arguments to <command>WriteMakefile()</command>, because they influence
what <filename>Makefile</filename> is being written by <command>ExtUtils::MakeMaker</command> 
after the user has invoked the usual build &amp; install procedure including the subsequent commands:
</para>

<example>
<title></title>
<programlisting>
% perl Makefile.PL
% make
% make test
# make install
</programlisting>
</example>

</sect1>

<sect1>
<title>Module::Build successor of ExtUtils::MakeMaker?</title>

<para>
As Ken Williams grew tired of <command>ExtUtils::MakeMaker</command> &amp; portability issues, he invented 
<command>Module::Build</command>, the designated successor of <command>ExtUtils::MakeMaker</command>, 
which is expected to run smoothly on most Operating Systems, because <command>Module::Build</command> 
takes advantage of creating Perl-valid syntax files only and does not rely upon crufty <filename>
Makefiles</filename>, which are often subject to misinterpretation, because so many incompatible 
flavours of <command>make</command> exist out there in the wild.

The current maintainer of ExtUtils::MakeMaker (Michael G. Schwern) elaborated about
this problematic some more in his talk 
<ulink url="http://mungus.schwern.org/~schwern/talks/MakeMaker_Is_DOOMED/slides/">MakeMaker is DOOMED</ulink>.
</para>

</sect1>

<sect1>
<title>Module::Build distribution "skeleton"</title>

<para>
If you take in consideration the distribution <command>HTML::Tagset</command> again, the rough skeleton
suitable for <command>Module::Build</command> having converted the <filename>Makefile.PL</filename> by 
<command>Module::Build::Convert</command> into a <filename>Build.PL</filename>, the output would be as 
following:
</para>

<example>
<title></title>
<programlisting>
# This -*-perl-*- program writes the Makefile for installing this distribution.
#
# See "perldoc perlmodinstall" or "perldoc ExtUtils::MakeMaker" for
# info on how to control how the installation goes.
# Note: this file has been initially generated by Module::Build::Convert 0.24_01

require 5.004;
use strict;
use warnings;

use Module::Build;

my $build = Module::Build->new
  (
   module_name => 'HTML::Tagset',
   dist_author => 'Andy Lester &lt;andy@petdance.com&gt;',
   dist_version_from => 'Tagset.pm',
   add_to_cleanup => [
                       'HTML-Tagset-*'
                     ],
   license => 'unknown',
   create_readme => 1,
   create_makefile_pl => 'traditional',
  );

$build->create_build_script;
</programlisting>
</example>

<para>
As you can see while <command>ExtUtils::MakeMaker</command> prefers upper-cased arguments,
<command>Module::Build</command> goes by entirely lower-cased arguments, which obey the rule 
of least surprise by being as intuitive as a description can be.
</para>

<para>The build and installation procedure is for a <command>Module::Build</command> distribution as following:</para>

<example>
<title></title>
<programlisting>
% perl Build.PL
% perl Build
% perl Build test
# perl Build install
</programlisting>
</example>

</sect1>

<sect1>
<title>Module::Build::Convert's state of operation</title>

<para>
<command>Module::Build::Convert</command> actually does all the background work and can be safely
considered the backend, whereas <command>make2build</command> may be looked at as practicle frontend utility.
<command>Module::Build::Convert</command> currently exposes two kinds of operation: static approach &amp; dynamic
execution. When static approach is being referred, this means ie. that the arguments contained within
the <filename>Makefile.PL's</filename> <command>WriteMakefile()</command> call are being parsed, whereas dynamic
execution "runs" the <filename>Makefile.PL</filename> and captures the arguments provided to <command>WriteMakefile()
</command>.
</para>

<para>
<command>Module::Build::Convert</command> parses statically by default, because the dynamic execution has the
downside that code will be interpreted and the interpreted output will be written to the <filename>Build.PL</filename>,
so you have to conclude that the user of the distribution will end up with predefined values computed
on the authors system; this is something that should surely be avoided whenever possible! If the
parsing approach fails, ie. loops endlessly on input, <command>Module::Build::Convert</command> will be "reinited"
again and dynamic execution of the <filename>Makefile.PL</filename> will be performed instead.
</para>

</sect1>

<sect1>
<title>Data section</title>

<para>
<command>Module::Build::Convert</command> comes with a rather huge data section, that contains the argument
conversion table, default arguments, sorting order, begin &amp; end code. If you wish to change
this data, you should consider making a <filename>~/.make2buildrc</filename> that will consist of this data by
launching <command>make2build</command> with the <command>-rc</command> switch. You shouldn't (!)
edit the data section within <command>Module::Build::Convert</command> directly, unless you are sure you want
to submit a patch.
</para>

<para>The sections are enlisted as:</para>

<sect2>
<title>Argument conversion</title>

<para>
On the left-hand side is the <command>MakeMaker's</command> argument name, on the right-hand side
the <command>Module::Build's</command> equivalent.
</para>

<example>
<title></title>
<programlisting>
NAME                  module_name
DISTNAME              dist_name
ABSTRACT              dist_abstract
AUTHOR                dist_author
VERSION               dist_version
VERSION_FROM          dist_version_from
PREREQ_PM             requires
PL_FILES              PL_files
PM                    pm_files
MAN1PODS              pod_files
XS                    xs_files
INC                   include_dirs
INSTALLDIRS           installdirs
DESTDIR               destdir
CCFLAGS               extra_compiler_flags
EXTRA_META            meta_add
SIGN                  sign
LICENSE               license
clean.FILES           @add_to_cleanup
</programlisting>
</example>

</sect2>

<sect2>
<title>Default arguments</title>

<para>Default <command>Module::Build</command> arguments to be added. Arguments with a leading # are ignored.</para>

<example>
<title></title>
<programlisting>
#build_requires       HASH
#recommends           HASH
#conflicts            HASH
license               unknown
create_readme         1
create_makefile_pl    traditional
</programlisting>
</example>

</sect2>

<sect2>
<title>Sorting order</title>

<para><command>Module::Build</command> arguments are sorted as enlisted herein.</para>

<example>
<title></title>
<programlisting>
module_name
dist_name
dist_abstract
dist_author
dist_version
dist_version_from
requires
build_requires
recommends
conflicts
PL_files
pm_files
pod_files
xs_files
include_dirs
installdirs
destdir
add_to_cleanup
extra_compiler_flags
meta_add
sign
license
create_readme
create_makefile_pl
</programlisting>
</example>

</sect2>

<sect2>
<title>Begin code</title>

<para>
Code that preceeds converted <command>Module::Build</command> arguments.
<command>$(UPPERCASE)</command> are stubs being substituted by <command>Module::Build</command> code.
</para>

<example>
<title></title>
<programlisting>
use strict;
use warnings;

use Module::Build;

$MAKECODE

my $b = Module::Build->new
$INDENT(
</programlisting>
</example>

</sect2>

<sect2>
<title>End code</title>

<para>
Code that follows converted <command>Module::Build</command> arguments.
<command>$(UPPERCASE)</command> are stubs being substituted by <command>
Module::Build</command> code.
</para>

<example>
<title></title>
<programlisting>
$INDENT);

$b->create_build_script;

$MAKECODE
</programlisting>
</example>

</sect2>

</sect1>

<sect1>
<title>make2build basic usage</title>

<para>
Making proper usage of <command>make2build</command> is as easy as launching it in the directory of the 
distribution which <filename>Makefile.PL</filename> you wish to convert.
</para>

<para>For example:</para>

<example>
<title></title>
<programlisting>
% make2build
</programlisting>
</example>

<para>
But you may also provide the full path to the distribution, assuming for example you didn't <command>cd</command>
directly into the distribution directory.
</para>

<example>
<title></title>
<programlisting>
% make2build /path/to/HTML-Tagset*
</programlisting>
</example>

<para>
In both cases the <filename>Makefile.PLs</filename> will be converted and no output is generated since <command>make2build
</command> acts quiet by default.
</para>

</sect1>

<sect1>
<title>make2build switches</title>

<para>
As <command>make2build</command> aims to be a proper script it, of course, provides both, the <command>-h</command> (help screen)
and <command>-V</command> (version) switches.
</para>

<example>
<title></title>
<programlisting>
% make2build -h
% make2build -V
</programlisting>
</example>

<para>
In case you end up with a mangled <filename>Build.PL</filename> written, you could examine the parsing process by
launching <command>make2build</command> with the <command>-d</command> switch which implies the pseudo-interactive
debugging mode.
</para>

<example>
<title></title>
<programlisting>
% make2build -d
</programlisting>
</example>

<para>
You may execute the <filename>Makefile.PL</filename> in first place, but such usage is deprecated since
<command>Module::Build::Convert</command> "downgrades" automatically when needed.
</para>

<example>
<title></title>
<programlisting>
% make2build -e (deprecated)
</programlisting>
</example>

<para>
Should you not like the indentation length or judge it to be to small, you can increase
it via the <command>-l</command> switch followed by an integer.
</para>

<example>
<title></title>
<programlisting>
% make2build -l length
</programlisting>
</example>

<para>
If you don't agree with the sorting order predefined in <command>Module::Build::Convert</command>, you may
enforce the native sorting order, which strives to arrange standard arguments with those seen
available in the <filename>Makefile.PL</filename>.
</para>

<example>
<title></title>
<programlisting>
% make2build -n
</programlisting>
</example>

<para>
The argument conversion table, default arguments to be added, the sorting order of the arguments,
the begin and end code aren't absolute either and may be changed by invoking <command>make2build</command> 
with the <command>-rc</command> switch standing for Resource Config file creation; the file will be created
in the home directory of the current user by the defaulted filename <filename>.make2buildrc</filename>.
</para>

<example>
<title></title>
<programlisting>
% make2build -rc
</programlisting>
</example>

<para>
While <command>make2build</command> is quiet by default, it may be switched to output some verbosity. There are two
verbosity level; to enforce verbosity level 1, launch <command>make2build</command> with <command>-v</command>, to 
enforce verbosity level 2, simply launch <command>make2build</command> with <command>-vv</command>.
</para>

<para>
Whenever <command>-v</command> was chosen, <filename>Makefile.PL</filename> arguments that were unknown to the
conversion table or are being skipped are being output. Whenever <command>-vv</command> was chosen, it will 
accumulate <command>-v</command> output &amp; the entire <filename>Build.PL</filename> written.
</para>

<example>
<title></title>
<programlisting>
% make2build -v
% make2build -vv
</programlisting>
</example>

</sect1>

</article>

